<?php

$config = new PrestaShop\CodingStandards\CsFixer\Config();

$config
    ->setUsingCache(false)
    ->getFinder()
    ->in(__DIR__)
    ->exclude(['vendor', 'tests/Resources/module-samples', 'tests/Resources/expected']);

return $config;
